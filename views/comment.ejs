<!DOCTYPE html>
<html>
    <head>
        <link href="/header.css" rel="stylesheet" type="text/css">
        <link href="/footer.css" rel="stylesheet" type="text/css">
        <link href="/comment.css" rel="stylesheet" type="text/css">
        <script src="/jquery/jquery-3.7.1.js"></script>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Forum</title>
    </head>

    <body>
        <div class="container">
            <%- include('header') %>
            <div class="main">
                <h3 class="successMsg">
                    조작이 성공했습니다.
                </h3>
                
                <h3 class="previousPage">
                    <a href="#">
                        이전 페지로
                    </a>
                </h3>
                
                <div class="articleContainer">                
                    <div class="articleWriter">
                        <img src="/img/uploads/<%= post.user.character %>">
                        <h3><%= post.user.name %></h3>
                    </div>

                    <div class="article">
                        <%= post.text %>
                    </div>
                </div>

            <% if(!session || session._id != post.user._id) { %>    
                <div class="myCommentContainer">
                    <h3>답변글</h3>
                    <form class="myCommentMain" id="commentForm"  data-post-id ="<%= post._id %>">
                        <textarea placeholder="답변글 내용">
                        </textarea>
                        <button type="submit" class="sendComment">답변하기</button>
                    </form>
                </div>
            <% } %>

                <% if(post.comments){ %>
                    <div class="commentsContainer">
                        <% post.comments.forEach(comment => { %>
                            <div class="commentContainer">
                                <div class="commentWriter">
                                    <%= comment.user.name %>
                                </div>
                                <form class="commentMain" data-comment-id="<%= comment._id %>" data-post-id="<%= post._id %>">
                                    <div class="commentText">
                                        <%= comment.text %>
                                    </div>
                                    <% if (session && (session._id.toString() === comment.user._id.toString())) {%>
                                        <button class="cancelBtn" type="submit">
                                            X
                                        </button>
                                    <% } %>
                                </form>
                            </div>
                        <% }) %>
                    </div>
                <% } %>
            </div>
            <%- include('footer') %>
        </div>
    </body>
    <script src="/js/comment.js"></script>
</html>